# AGENTS

先读 `.aidrp/repo-map.md` 和当前激活的工件，再决定是否扩大上下文。

## 必要工件

- `.aidrp/repo-map.json`
- `.aidrp/repo-map.md`
- `.aidrp/briefs/*.json`
- `.aidrp/tasks/*.json`
- `.aidrp/debug/*.json`
- `.aidrp/traces/*.json`
- `.aidrp/evals/*.json`

## 规则

- 需求还模糊时，先做 `discovery interview | 需求访谈` 并产出 `requirement-brief | 需求简报`
- 非 trivial 任务先生成 `task-packet | 任务包`
- bug 先生成 `debug-pack | 排障包`
- 当前工件解释不了问题时，才能扩大阅读范围
- 推理明显转向时，要写 `decision-trace | 决策轨迹`
- 确认修好的 bug，要生成 `eval-case | 回归用例`
